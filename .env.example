# ── Dominio ───────────────────────────────────────────────────────────────────
DOMAIN=victorpetruccio.online

# ── Base de datos ─────────────────────────────────────────────────────────────
DB_PASSWORD=CambiarPorPasswordSegura123!

# ── Token relay ERP ↔ VPS (debe coincidir con el configurado en el escritorio) ─
# Este token ya está fijo en application.properties del relay y del escritorio.
# Solo cambiarlo si se regenera en ambos lados a la vez.
RELAY_TOKEN=6fTfHx4l4xvP3artbY47Rm0TeG9k4489w0L1u4q8Q0y7lYQgjnv7gbzbnxYQs8HBN598SbA3

# ── JWT ───────────────────────────────────────────────────────────────────────
JWT_SECRET=CambiarPorSecretLargoAleatorio256bits

# ── Google OAuth (opcional — dejar vacío para deshabilitar) ───────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
